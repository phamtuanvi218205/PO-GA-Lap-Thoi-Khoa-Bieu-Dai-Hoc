# Copy these variable names into the IDE run configuration or operating-system
# environment. Spring Boot does not automatically load this file by itself.

DB_URL=jdbc:sqlserver://localhost:1433;databaseName=timetabling_gapo;encrypt=true;trustServerCertificate=true
DB_USERNAME=your_database_username
DB_PASSWORD=your_database_password

# Add these only when the corresponding features are implemented.
JWT_SECRET=replace_with_a_long_random_secret
GA_PO_SERVICE_URL=http://localhost:8000